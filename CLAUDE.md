# CLAUDE.md

`docs/index.md`(公開ページ、英語)を保守する際の作業手順。

## 用語

DWG7は "domain working group"(**discussion**ではない)。hfuさんから2026-09-06に訂正指示あり。

## 更新頻度

おおむね週1回程度。前回更新から日が浅ければ無理に更新しなくてよい。

## 手順

1. dwg7組織でGitHub Pagesが有効なリポジトリを機械的に列挙する:
   ```
   gh api orgs/dwg7/repos --paginate --jq '.[] | select(.has_pages==true) | .name'
   ```
2. 各リポジトリについて `https://dwg7.github.io/<repo>/` が実際にHTTP 200で
   アクセス可能か確認する(`curl -s -o /dev/null -w "%{http_code}"`)。404や五百番台は除外。
3. 掲載候補を選ぶ基準:
   - 対外的な訴求力があるか(国際的な読み手が見て価値を理解できるか)
   - 「client test」「demo」など明らかに未完成・内部検証段階と分かる表示のものは、
     文脈上どうしても必要な場合(例: 特定の連携を裏付ける具体例)を除き基本的に見送る
   - 内部向けツール(特定機関の職員向け等)や、GIS成果というより別領域(フリート運用等)の
     ものは、このページの主旨(DWG7の対外的な成果)からは外す
   - 全件を機械的に載せるのではなく、多様性(防災・実務ツール・国際開発・研究等)を
     意識して絞る。目安は6〜10件程度
4. `docs/index.md`は**英語のみ**(公開ページの言語はREADME.md/CLAUDE.mdと異なる)。
   各項目は1行程度の簡潔な説明にとどめる。
5. 末尾の「Last updated」を更新した日付に書き換える。
6. commit・push。GitHub Pagesは通常数分でビルドされる。`https://dwg7.github.io/`に
   実際にアクセスして反映を確認する。

## 判断に迷ったら

新しいリポジトリが増えていた・既存の説明が古くなっていた等、載せる内容に実質的な変更が
無ければ、その回は「Last updated」の書き換えだけで良い(無理に文面を変える必要はない)。
逆に、大きな判断(特定プロジェクトを外す・新しい観点で再構成する等)は、
[UNopenGIS/7#996](https://github.com/UNopenGIS/7/issues/996)のコメントで示した内容との
整合性を意識すること。
